name: Terraform refreshing 

on:
  workflow_dispatch
  #push:
  #  branches:
  #    - Master
  #    - development
  #    - staging


jobs:
  terraform-refreshing:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.0
          
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_KEY }}
          aws-region: ap-south-1

      - name: Set Terraform Workspace
        id: workspace
        run: |
          if [[ "${GITHUB_REF##*/}" == "Master" ]]; then
            echo "::set-output name=workspace::production"
          elif [[ "${GITHUB_REF##*/}" == "development" ]]; then
            echo "::set-output name=workspace::development"
          elif [[ "${GITHUB_REF##*/}" == "staging" ]]; then
            echo "::set-output name=workspace::staging"
          else
            echo "::set-output name=workspace::default"
          fi

      - name: Terraform Init
        run: terraform init

      - name: Force Unlock State
        run: terraform force-unlock ea578d7b-4b38-4643-5817-c5fe0c9f6775 || true -auto-approve
        
      - name: Refresh Terraform State
        run: terraform plan -refresh-only
      
